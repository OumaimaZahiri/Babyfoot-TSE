if !CondBut && !CondHT
	if balle in but
		But
		CondBut true

	if balle HorsTerrain
		Sortie à frame
		CondHT true

	if pos idem frame prec (balle perdue)
		compteurLost++
		if compteurLost>X (30?)
			if près but
				Ambuiguité
	else 
		compteurLost = 0;
else 
	if balle in terrain
		compteurInTer++
		if compteurInTer>X (5?)
			if CondBut && temps last event < X frames (20?)
				Gamelle
			CondBut = false;
			CondHT= false;
			compteurInTer = 0;
	else
		compteurInTer = 0;
		